// Payments tab is static HTML, no rendering needed
// This module exists for consistency with other tabs

export function initPaymentsModule() {
    // Payments page is static, no initialization needed
}

export function renderPayments() {
    // Payments page is static HTML, no rendering needed
    // Content is already in index.html
}

