# 🔍 АНАЛИЗ ПРОБЛЕМ СО СКРОЛЛИНГОМ

**Дата:** 29 января 2026  
**Статус:** ✅ Задача 1.1 ЗАВЕРШЕНА

---

## 🎯 ОБНАРУЖЕННЫЕ ПРОБЛЕМЫ

### 1. **Body Padding конфликтует с Fixed Header**

**Файл:** `src/styles/main.css` (строки 467-468)

```css
body {
    padding-top: var(--header-total-height);  /* 170px на десктопе, 150px на мобильных */
    margin-top: 0;
}
```

**Проблема:** 
- Body имеет огромный padding-top (170px)
- Это создает пустое пространство сверху
- При скролле вверх видна пустая зона

---

### 2. **Overflow-X Hidden на Body блокирует нормальный скролл**

**Файл:** `src/styles/main.css` (строка 464)

```css
body {
    overflow-x: hidden;  /* ❌ Блокирует горизонтальный скролл, но может влиять на вертикальный */
}
```

**Проблема:**
- overflow-x: hidden может создавать проблемы с вертикальным скроллом
- Лучше использовать max-width для предотвращения горизонтального скролла

---

### 3. **MainContent не имеет явного overflow-y: auto**

**Файл:** `src/components/layout/MainContent.svelte`

```svelte
<main class="w-full overflow-x-hidden">
  <!-- content -->
</main>

<style>
  main {
    padding: 0;
  }
</style>
```

**Проблема:**
- Нет явного overflow-y: auto
- Нет min-height или height для контейнера
- Скролл не работает правильно внутри контейнера

---

### 4. **Sidebar имеет height: calc(100vh - 64px)**

**Файл:** `src/styles/main.css` (строки 3580-3586)

```css
#mini-sidebar {
    height: calc(100vh - 64px);
    position: sticky;
    top: 64px;
    overflow-y: auto;
    overflow-x: hidden;
}
```

**Проблема:**
- Использует старое значение 64px вместо var(--header-total-height)
- Может создавать конфликты с высотой

---

### 5. **Tabs компоненты имеют лишние padding**

**Файлы:** 
- `src/components/tabs/DashboardTab.svelte`
- `src/components/tabs/ExpensesTab.svelte`
- `src/components/tabs/DebtsTab.svelte`
- `src/components/tabs/RecurringExpensesTab.svelte`

**Проблема:**
- Каждый tab имеет padding: 1.5rem
- Это создает пустое пространство сверху
- Нужно уменьшить или убрать верхний padding

---

### 6. **DashboardTab имеет дублирующий Date/Time блок**

**Файл:** `src/components/tabs/DashboardTab.svelte` (строки 140-155)

```svelte
<!-- Center: Date & Time (Prominent) -->
<div id="dashboard-datetime" class="flex flex-col items-center justify-center text-center order-first lg:order-none mb-4 lg:mb-0">
  <div id="dash-time" class="text-2xl md:text-3xl font-bold text-gray-800 dark:text-gray-200 leading-none tracking-tight text-glow">
    {dashTime}
  </div>
  <div id="dash-date" class="text-xs md:text-sm font-medium text-gray-500 dark:text-gray-400 uppercase tracking-widest mt-1">
    {dashDate}
  </div>
</div>
```

**Проблема:**
- Дата и время дублируются (есть в Header и в DashboardTab)
- Занимает лишнее место
- Создает визуальный шум

---

## 📋 ПЛАН ИСПРАВЛЕНИЙ

### Приоритет 1: Исправить Body и Overflow
1. Убрать `overflow-x: hidden` с body
2. Уменьшить `padding-top` на body (оставить минимальный для header)
3. Добавить `overflow-y: auto` на main content контейнер

### Приоритет 2: Исправить MainContent
1. Добавить явный `overflow-y: auto` на main
2. Установить правильную высоту контейнера
3. Убедиться что контент скроллится внутри

### Приоритет 3: Убрать пустые зоны
1. Уменьшить padding на tabs компонентах
2. Убрать gap между header nav и content
3. Убрать дублирующий date/time блок из DashboardTab

### Приоритет 4: Исправить Sidebar
1. Обновить height calc на использование CSS переменных
2. Проверить sticky positioning

---

## 🎯 СЛЕДУЮЩИЙ ШАГ

Переходим к **Задаче 1.2** - Исправление скроллинга в контейнере контента.

---

## ✅ СТАТУС: АНАЛИЗ ЗАВЕРШЕН

Все проблемные места идентифицированы и задокументированы.
