## Цели
- Выполнить полную ревизию проекта (ошибки/варнинги/баги) и устранить.
- Переработать header на десктопе и мобайле по вашим требованиям, чтобы всё помещалось и ничего не обрезалось.

## Ревизия проекта
1. Сборка и предпросмотр: запустить build/preview, собрать консольные ошибки.
2. JS-аудит: обработчики табов, тема/язык/валюта, радио/погода, глобальные клики; устранить исключения и некорректные состояния.
3. Стили: выявить и удалить дубли мобильных правил; унифицировать медиазапросы.
4. Локализация/переводы: проверить переключение языков и корректность UI-текстов.

## Header — Десктоп (как указали)
- Сетка: колонки `logo(фикс) | 1fr | auto*` с предсказуемыми gap.
- Логотип: ограничить размер в колонке, сохранить аспект, убрать конфликтные hover-эффекты.
- Вкладки: `overflow-x: auto` при переполнении, сохранение активной вкладки.
- Правые иконки: выравнивание по правому краю, минимальные размеры кнопок.

## Header — Мобайл (исправлено согласно вашим пожеланиям)
- Двухрядная сетка:
  - Ряд 1: логотип слева, слоган (мото) рядом, правые иконки справа.
  - Ряд 2: погода слева, радио справа.
- Компактные размеры: логотип ≈40–44px; у радио скрыть эквалайзер, у погоды скрыть длинное название города (оставить компакт);
- Мобильное меню вкладок: `toggle + menu` со списком кнопок `data-tab`, закрытие по клику/по клику вне.
- Мини-сайдбар: скрыт по умолчанию, доступен через бургер как резервная навигация.

## Стили и адаптивность
- Удаление устаревших мобильных дублей и конфликтов в `main.css`.
- Единые брейкпоинты (`≤480`, `641–1024`, `≥1025`) и согласованные правила для `logo/motto/weather/radio/right-icons/tabs`.
- Для вкладок: тач-инерция скролла и визуальное выделение active.

## Доступность
- `aria-haspopup/aria-expanded` для мобильного меню вкладок.
- Корректные `aria-label` для кнопок и фокус-стили.

## Тестирование
1. Проверка брейкпоинтов на мобайл/планшет/десктоп.
2. Переполнение вкладок: длинные локализации, горизонтальная прокрутка.
3. Переключения темы/языка/валюты.
4. Видимость погоды/радио и отсутствие обрезаний.

## Поставка
- Изменения в `index.html`, `src/js/app.js`, `src/styles/main.css` по вышеуказанным пунктам.
- Верификация в превью и демонстрация результата.

Подтвердите план — начну реализацию: зачистка мобильных дублей, новая сетка (с включённым слоганом на мобайле), мобильное меню вкладок и регрессионные проверки.